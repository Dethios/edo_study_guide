# Agents

## Codex (primary)

- Always read `Other/project_export.md` and `Other/project_export.json` first.
- Follow the Workspace Instructions above.
- When done with a task, output:
  1) a short summary of completed job;
  2) context of current chat and
  3) patch `Other/project_export.json` to update project memory.
  4) run a test compilation for document with `latexmk -shell-escape

## Jobs

When all other tasks are complete, please complete outstanding tasks below. Mark them complete when finished with each task:

- [ ] Review entire scope of project, including every file in all folders and update `Other/project_export.json` to current project status.
